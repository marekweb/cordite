{
  "name": "express-cordite",
  "version": "0.1.0",
  "main": "index.js",
  "scripts": {
    "test": "node test",
    "format": "prettier --write --single-quote '**/*.{js,json}'"
  },
  "license": "MIT",
  "peerDependencies": {
    "express": "^4.0.0"
  },
  "dependencies": {
    "compression": "^1.7.1",
    "express": "^4.16.2",
    "helmet": "^3.9.0"
  },
  "devDependencies": {
    "prettier": "^1.9.2"
  }
}
